{
  "name": "jensroemer-astro",
  "type": "module",
  "version": "0.0.1",
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "lint": "eslint src",
    "test:ui": "npx playwright test --ui",
    "test": "docker run --rm --network host -e COREPACK_ENABLE_DOWNLOAD_PROMPT=0 -v $(pwd):/work/ -v /work/node_modules -w /work/ -it mcr.microsoft.com/playwright:v1.60.0-noble /bin/bash -c 'corepack enable && pnpm i && pnpm exec playwright test'",
    "test:update": "docker run --rm --network host -e COREPACK_ENABLE_DOWNLOAD_PROMPT=0 -v $(pwd):/work/ -v /work/node_modules -w /work/ -it mcr.microsoft.com/playwright:v1.57.0-noble /bin/bash -c 'corepack enable && pnpm i && pnpm exec playwright test --update-snapshots'",
    "generate-mock": "ts-generate-mock-data --output-dir ./tests/mocks"
  },
  "dependencies": {
    "@astrojs/mdx": "6.0.3",
    "@astrojs/rss": "4.0.19",
    "@astrojs/solid-js": "6.0.1",
    "astro": "^6.4.8",
    "solid-js": "1.9.12"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "1.60.0",
    "@types/node": "26.1.1",
    "eslint": "9.39.2",
    "eslint-plugin-astro": "1.6.0",
    "ts-generate-mock-data": "0.0.2",
    "typescript-eslint": "8.57.2"
  }
}
